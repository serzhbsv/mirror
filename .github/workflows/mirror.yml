name: Зеркало footbook.ru

on:
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    
    steps:
      - name: Авторизация и получение кук
        run: |
          # Шаг 1: Получаем куки с главной
          curl -k -4 --connect-timeout 60 --max-time 300 \
               -L https://footbook.ru/index.php \
               -c cookies.txt -b cookies.txt -o /dev/null
          
          # Шаг 2: Отправляем форму логина
          curl -k -4 --connect-timeout 60 --max-time 300 \
               -X POST https://footbook.ru/login.php \
               -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
               -H "Content-Type: application/x-www-form-urlencoded" \
               -H "Referer: https://footbook.ru/login.php" \
               -d "login=${{ secrets.SITE_LOGIN }}" \
               -d "password=${{ secrets.SITE_PASSWORD }}" \
               -d "remember=on" \
               -b cookies.txt \
               -c cookies.txt \
               -L
          
          echo "=== Куки получены ==="
          cat cookies.txt
      
      - name: Скачивание сайта (без logout)
        run: |
          wget --inet4-only \
               --no-check-certificate \
               --timeout=60 \
               --tries=5 \
               --mirror \
               --page-requisites \
               --convert-links \
               --adjust-extension \
               --no-parent \
               --domains=footbook.ru \
               --load-cookies cookies.txt \
               --keep-session-cookies \
               --header="User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
               --header="Referer: https://footbook.ru/" \
               --wait=2 \
               --limit-rate=200k \
               --reject-regex=".*logout.*|.*signout.*|.*exit.*" \
               --exclude-directories="logout,exit,signout" \
               https://footbook.ru
          
          echo "=== Результат ==="
          du -sh ./footbook.ru 2>/dev/null || echo "Папка не создана"
          find ./footbook.ru -type f 2>/dev/null | wc -l
      
      - name: Архивация и разбивка
        run: |
          if [ -d "./footbook.ru" ] && [ "$(ls -A ./footbook.ru)" ]; then
            tar -czf footbook-mirror.tar.gz ./footbook.ru
            split -b 500M footbook-mirror.tar.gz "footbook-part-" -a 2
            ls -lh footbook-part-*
          else
            echo "⚠️ Папка пуста!"
            exit 1
          fi
      
      - name: Загрузка артефактов
        uses: actions/upload-artifact@v4
        with:
          name: footbook-mirror-parts
          path: footbook-part-*
          retention-days: 7
